const { Comment } = require('../models');

const postComment = async (req, res, next) => {
    try {
        
    } catch (error) {
        next(error);
    }
};
const getAllCommentByRecipeId = async (req, res, next) => {
    try {
        
    } catch (error) {
        next(error);
    }
};

module.exports = {
    postComment, getAllCommentByRecipeId
};